{
  "name": "@bkey-example/vault-access",
  "version": "0.1.0",
  "private": true,
  "description": "CLI that stores and retrieves end-to-end encrypted secrets from a BKey vault via biometric approval.",
  "type": "module",
  "bin": {
    "bkey-vault-example": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@bkey/sdk": "^0.1.0",
    "@noble/curves": "^1.8.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0"
}
