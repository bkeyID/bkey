# BKey API base URL. Use https://api.bkey.id for production.
BKEY_API_URL=https://api.bkey.id

# OAuth client credentials for the caller.
# Register a client at https://bkey.id and paste the values here.
BKEY_CLIENT_ID=
BKEY_CLIENT_SECRET=
