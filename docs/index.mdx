---
title: Introduction
description: BKey — cryptographic proof-of-humanity, anywhere. Think Google Authenticator, with your face, and for anything that matters.
---

<Warning>
  BKey is in **active development**. APIs and SDKs may change without notice.
</Warning>

# BKey Developer Docs

Think of BKey as **Google Authenticator — with your face, and for anything**.

Google Authenticator proves you know a 6-digit code. BKey proves a specific human approved a specific action — right now, on a registered device, with their real face. Every approval produces an **EdDSA-signed JWT** your app can verify and keep as a permanent audit trail.

## What you can build with it

BKey turns "a real human approved this" into a one-call primitive. A few concrete shapes:

### Real proof-of-humanity, without the CAPTCHA guessing game

"Select all the stoplights" doesn't prove a human did it — bots solve them and real users hate them. BKey does: for users signed up once with BKey, any sensitive form submission, signup, comment, or download can be gated on a face-verified approval. You get back a cryptographic signature, not a probabilistic bot score.

### Agent actions that can't silently go rogue

Your AI agent can draft a deploy, a refund, a payment, or a database change — but the action runs only after a human approves on their phone with Face ID. The agent never holds long-lived credentials that can spend money or touch production on their own.

### Admin & compliance gates

Role grants, table drops, wire transfers, support overrides — gate any destructive action on a biometric approval. Every operation produces a signed, timestamped attestation bound to a specific human's device. Auditable by default.

### MCP tool gating

Your AI assistant invokes a tool, your server pauses, the operator's phone lights up, they approve with Face ID, the tool runs. Every tool invocation in your MCP server carries cryptographic evidence of consent. See [MCP integration](/guides/mcp-integration).

### Agent payments (x402 + MPP)

Your agent hits a paid API (HTTP 402), BKey auto-detects the protocol (x402 on-chain USDC or MPP Stripe SPT), handles spending limits + biometric approval + signing, and the agent retries with the signed credential. See [agent payments](/guides/x402-payments).

### Encrypted vault for secrets

End-to-end encrypted secrets released only after biometric approval — the plaintext never touches the server. See [encryption](/guides/encryption).

### Document & transaction signing

Sign legal documents or crypto transactions with a biometric-bound key that lives in the phone's secure enclave. Private keys never leave the device.

## The core primitive: one line of code

```typescript
const result = await bkey.approve('Delete user alice@corp', {
  scope: 'approve:action',
});
// result.accessToken is an EdDSA JWT proving the human approved this on their phone
```

That's the whole pattern. Drop it anywhere you need a real human in the loop. See [CIBA](/authentication/ciba) for the one-line approval in Python, TypeScript, and the CLI.

## Under the hood

- **CIBA** (RFC 8958) for backchannel biometric approval
- **EdDSA (Ed25519)** signed JWTs — verify with our JWKS
- **On-device keys** in the iOS Secure Enclave / Android Keystore — private keys never leave the phone
- **End-to-end encrypted** vault payloads (X25519 ECDH + AES-256-GCM)
- **Standards-compliant OIDC** — you don't need our SDK; any OIDC-aware library works

## Start here

| Path | When |
|---|---|
| [Quickstart](/quickstart) | Get to a working `approve()` call in 5 minutes |
| [Integrate BKey](/authentication/integrating-bkey) | Our endpoint surface, for integrators who don't want to use our SDK |
| [CIBA one-line approval](/authentication/ciba) | The core SDK pattern |
| [Agent payments](/guides/x402-payments) | x402 + MPP auto-detection |
| [MCP integration](/guides/mcp-integration) | Gate MCP tool calls on biometrics |
| [Encryption](/guides/encryption) | What's protected at each step |

## SDKs

<CodeGroup>
```bash npm
npm install @bkey/sdk
```

```bash pip
pip install bkey-sdk
```

```bash CLI
npm install -g @bkey/cli
```
</CodeGroup>

Go and Rust SDKs are coming. See the [GitHub repo](https://github.com/bkeyID/bkey) for updates.
